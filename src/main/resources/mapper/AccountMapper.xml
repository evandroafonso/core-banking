<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tuum.corebanking.account.mapper.AccountMapper">

    <resultMap id="AccountResultMap" type="Account">
        <id property="id" column="id"/>
        <result property="businessId" column="business_id"/>
        <result property="country" column="country"/>
        <result property="customerId" column="customer_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO accounts (business_id, country, customer_id, created_at, updated_at)
        VALUES (#{businessId}, #{country}, #{customerId}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findById" resultMap="AccountResultMap">
        SELECT id, business_id, country, customer_id, created_at, updated_at
        FROM accounts
        WHERE id = #{id}
    </select>

    <select id="findByBusinessId" resultMap="AccountResultMap">
        SELECT id, business_id, country, customer_id, created_at, updated_at
        FROM accounts
        WHERE business_id = #{businessId}
    </select>

    <select id="findByCustomerId" resultMap="AccountResultMap">
        SELECT id, business_id, country, customer_id, created_at, updated_at
        FROM accounts
        WHERE customer_id = #{customerId}
        ORDER BY created_at DESC
    </select>

    <select id="findAccountIdByBusinessId" resultType="Long">
        SELECT id
        FROM accounts
        WHERE business_id = #{businessId, jdbcType=OTHER}
    </select>

</mapper>